import 'package:flutter/material.dart';

class HistoriqueLocation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Center(
        child: new Text("Historique location"),
      ),
    );
  }
}
